<UserControl x:Class="C19K.Wpf.Views.HistoryCaseReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:C19K.Wpf.Views"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Controls:ToggleSwitch Grid.Column="3" OnLabel="Show District Details" OffLabel="Show State Wide" IsChecked="{Binding ShowDistrictDetails}" HorizontalAlignment="Right" />

        <oxy:PlotView Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" Model="{Binding ActiveCaseGraphModel}" Controller="{Binding ChartController}"/>
    </Grid>
</UserControl>
